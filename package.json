{
  "name": "colonist-card-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension: card tracker for colonist.io (WebSocket based)",
  "scripts": {
    "test": "node --test",
    "live": "node tools/live-capture.mjs",
    "i18n": "node tools/i18n-build.mjs",
    "zip": "node tools/i18n-build.mjs --check && node tools/pack.mjs"
  },
  "devDependencies": {
    "playwright": "^1.50.0"
  }
}
